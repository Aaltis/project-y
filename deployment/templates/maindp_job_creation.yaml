apiVersion: batch/v1
kind: Job
metadata:
  name: postgres-init
  annotations:
    "helm.sh/hook": post-install,post-upgrade  # Run AFTER install to avoid ordering issues
    "helm.sh/hook-weight": "5"                 # Ensure it runs after ConfigMap is created
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: postgres-init
        image: postgres:13
        command: ["/bin/sh", "-c"]
        args:
          - |
            echo "⏳ Waiting for PostgreSQL to be ready..."
            until pg_isready -h postgres -U postgres -p 5432; do
              echo "🔄 Still waiting for PostgreSQL..."
              sleep 5
            done
            echo "✅ PostgreSQL is ready!"
            echo "🚀 Running init.sql..."
            PGPASSWORD="${POSTGRES_PASSWORD}" psql -h postgres -U postgres -f /docker-entrypoint-initdb.d/init.sql
            echo "✅ Database initialization completed!"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: main_database_user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: main_database_password
        volumeMounts:
        - name: init-sql
          mountPath: /docker-entrypoint-initdb.d
      volumes:
      - name: init-sql
        configMap:
          name: postgres-init-sql
